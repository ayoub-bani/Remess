$(document).ready(function () {
    $('.owl-carousel').owlCarousel({
        autoplay: {
            Type: Boolean,
            Default: true,
        },
        loop: {
            Type: Boolean,
            Default: true,
        },
    });
});